<% provide(:title, 'Add a student') %>
<h1>New Student</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @student do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit  "Add Student", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>